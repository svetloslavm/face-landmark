/**
 * Represents the resolution of an object with width and height dimensions.
 *
 * @property {number} width - The width of the resolution.
 * @property {number} height - The height of the resolution.
 */
export interface Resolution {
  width: number;
  height: number;
}
